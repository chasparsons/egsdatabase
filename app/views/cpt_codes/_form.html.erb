<%= form_for(cpt_code) do |f| %>
  <% if cpt_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cpt_code.errors.count, "error") %> prohibited this cpt_code from being saved:</h2>

      <ul>
      <% cpt_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cats %>
    <%= f.text_field :cats %>
  </div>

  <div class="field">
    <%= f.label :mouse %>
    <%= f.text_field :mouse %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
