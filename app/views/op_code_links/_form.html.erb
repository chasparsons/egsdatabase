<%= form_for(op_code_link) do |f| %>
  <% if op_code_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(op_code_link.errors.count, "error") %> prohibited this op_code_link from being saved:</h2>

      <ul>
      <% op_code_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :has_many %>
    <%= f.text_field :has_many %>
  </div>

  <div class="field">
    <%= f.label :order %>
    <%= f.number_field :order %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
